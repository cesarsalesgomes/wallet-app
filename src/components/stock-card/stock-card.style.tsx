import { makeStyles } from '@material-ui/core';

const stockCardStyles = makeStyles(() => ({
  root: {
    minWidth: 400,
  },
  title: {
    fontSize: 18
  },
  subTitle: {
    fontSize: 15
  }
}));

export default stockCardStyles;
